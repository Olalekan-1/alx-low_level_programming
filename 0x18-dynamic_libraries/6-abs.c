#include "main.h"

/**
* _abs - Entry point _ print the absolute number
* Return: The absolute numbers
* @n: the return number
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	return (-n);
}

