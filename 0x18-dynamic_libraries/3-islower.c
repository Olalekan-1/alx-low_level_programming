#include "main.h"

/**
* _islower - To check if it is a lower character
* @c: character to be checked
* Return: return 1 if c is lower character, return 0 if otherwise
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	return (0);

}

